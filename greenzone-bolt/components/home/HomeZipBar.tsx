"use client";

/**
 * ZIP + “Near me” + cart live in the site header (`Header.tsx` bottom row). Homepage categories sit in the hero strip.
 * This component is a no-op shim so stale bundler caches or old imports don’t break the build.
 */
export function HomeZipBar() {
  return null;
}
